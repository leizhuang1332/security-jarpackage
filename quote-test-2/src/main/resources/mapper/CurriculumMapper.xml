<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lz.mapper.CurriculumMapper">

    <select id="selectList" resultType="com.lz.bean.Curriculum">
        SELECT * FROM curriculum WHERE (del = 0 OR del IS NULL)
    </select>
</mapper>